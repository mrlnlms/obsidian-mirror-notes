> [!success] CATEGORIES MATCH
> Este mirror apareceu via **filterProps** na chave `categories`.
> Nota: **{{title}}**
>
> Antes (parser manual), listas iam TODAS pra `tags` independente da chave.
> Se voce ve este callout, o bug foi corrigido.
