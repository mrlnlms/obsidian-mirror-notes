---
title: Below Properties Test
type: position-test
status: active
priority: medium
tags:
  - test
  - positions
---

# Conteudo da nota

> [!warning] Deprecated — usar `Top / Below properties (CM6)` no dropdown
> A posicao `below-properties` (DOM) esta deprecated desde v42. CM6 `top` produz resultado visual identico com melhor performance e reatividade. O codigo DOM permanece como fallback.

Esta nota testa a posicao `below-properties` (DOM legacy). O callout deve aparecer abaixo das properties.

Linha extra pra dar corpo ao editor.



```mirror
template: templates/positions/below-properties.md
```
