---
title: Fallback Props Hidden
type: fallback-test
tags:
  - test
  - fallback
---

# Teste de Fallback — Properties ocultas

> [!warning] Deprecated — `below-properties` DOM agora e fallback de CM6 `top`
> Desde v42, usar `Top / Below properties` no dropdown. O codigo DOM permanece funcional como fallback.

Este mirror esta configurado como `below-properties` (DOM). Com properties ocultas, faz fallback para CM6 `top`.
