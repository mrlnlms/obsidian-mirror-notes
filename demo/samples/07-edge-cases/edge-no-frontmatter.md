# Nota sem frontmatter

Esta nota **nao tem YAML**. Serve pra testar se posicoes CM6 (top/bottom) funcionam sem frontmatter e se DOM positions fazem fallback corretamente.

Linha extra pra dar corpo ao editor.


```mirror
template: Teste MCM.md
```
