# Nota sem YAML — teste above-title

Esta nota **nao tem frontmatter**. Testa dois cenarios simultaneos:

1. `above-title` (DOM) — o `.inline-title` existe sem YAML?
2. `{{title}}` — sem frontmatter, aparece literal ou faz fallback?

Alternar Settings > Appearance > Show inline title ON/OFF e observar.
x